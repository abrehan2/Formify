// Imports:
import { Button } from '@/components/ui/button';
import { SaveIcon } from 'lucide-react';

export default function SaveButtonForm() {
  return (
    <Button variant={'outline'} size={'icon'}>
      <SaveIcon />
    </Button>
  );
}
