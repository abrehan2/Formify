// Imports:
import { Button } from '@/components/ui/button';

export default function PublishFormBtn() {
  return (
    <Button
      variant={'ghost'}
      className="bg-gradient-to-r from-indigo-400 to-orange-400 text-white hover:text-white"
    >
      Publish
    </Button>
  );
}
